import { StudentStatusDirective } from './student-status-directive';

describe('StudentStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new StudentStatusDirective();
    expect(directive).toBeTruthy();
  });
});
